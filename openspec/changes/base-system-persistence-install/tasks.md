## 1. VM test harness on the real layout

- [x] 1.1 Create `tests/values.nix` per design D7 (plaintext `ssid`, `psk`, `password` and `hash`, the hash generated once with `nix shell nixpkgs#mkpasswd -c mkpasswd -m yescrypt`, since `mkpasswd` is not in the devShell; test-only, not secrets); rewrite `tests/default.nix` as a NixOS module that imports it and sets `disko.tests.extraChecks` and leaves `disko.tests.extraConfig` empty (the harness fixes the booted VM at 1 GB RAM and exposes no memory option, so none is set; its `meta.timeout = 600` is a Hydra hint, the driver's bound is 3600 s); point `checks.x86_64-linux.vm` at `(nixosConfigurations.emubox.extendModules { modules = [ ./tests ]; }).config.system.build.installTest` in `flake.nix`, and add `checks.x86_64-linux.closure-no-secrets` per design D7 (grep over `closureInfo` rooted at that same `extendModules` expression's `config.system.build.toplevel` for the `psk` and `hash` imported from `tests/values.nix`); every test override lives at the test module's top level and `disko.tests.extraConfig` stays empty
- [x] 1.2 Write the first assertion group in `extraChecks`, with every boot phase (initial, after `start()`, after `crash()`/`start()`) beginning with `machine.wait_for_unit("multi-user.target")` because the harness itself only waits for `local-fs.target`: `/`, `/nix`, `/persist`, `/data`, `/data/cache` are btrfs subvolume mounts, `bootctl status` reports systemd-boot, `id player` succeeds, `/data/home/player` exists, and each of `/data/{home,roms,bios,saves,es-de,media,cache}` exists with the owner and mode the tmpfiles layout declares; confirm the install test runs in CI (design Context: CI is the runner, since the derivation requires the `kvm` feature and no local builder offers it) within the harness's 1 GB memory and the driver's 3600 s global timeout (the display manager is already forced off at the test module's top level per design D7; a run that hits the timeout means QEMU fell back to TCG, so the udev step from 7.2 did not make `/dev/kvm` openable)
- [x] 1.3 Add the persistence assertions (red): record `/etc/machine-id`, write `/root/marker`, `/etc/marker`, `/data/marker`, `/tmp/marker`, `shutdown()`/`start()`, assert `/root`, `/etc` and `/tmp` markers gone, `/data` marker present, machine-id unchanged, `journalctl --list-boots` shows two boots, and `findmnt --target /var/log` reports a SOURCE ending in `[/@persist/var/log]` (on btrfs findmnt shows the subvolume path in brackets, never a `/persist/...` path)
- [x] 1.4 Add the power-cut assertion: write `/root/marker` again, `crash()` then start, node reaches `multi-user.target` with no repair prompt and the marker is gone

## 2. Ephemeral root

- [x] 2.1 Verify (settled at evaluation against the pinned inputs: `fileSystems."/".device` and `boot.initrd.systemd.initrdBin`; the VM run would only re-confirm) that the root partition carries disko's `disk-main-root` label and that `btrfs` is on the initrd PATH; if not, add `boot.initrd.systemd.extraBin.btrfs`
- [x] 2.2 Add `boot.initrd.systemd.services.rollback-root` to `modules/persistence` per design D1 (mount top level by partlabel, delete nested subvolumes, delete and recreate `@root`)
- [x] 2.3 Drop the RSA host key entries from the persisted list and set `services.openssh.hostKeys` to ed25519 only; keep the directory list as scaffolded
- [x] 2.4 Run the VM test in CI (the pull-request run from 7.3): 1.3 and 1.4 go green. Run 1 (2026-08-30): KVM and the install worked; the rollback unit ran a second time in the switch-root transaction and deleted the live `@root` (design D1); fixed with `RemainAfterExit` on the three initrd oneshots and `ConditionPathIsMountPoint=!/sysroot` on the rollback. Run 2: boots to `multi-user.target` with no failed unit, layout, loader, `/run` and `player` subtests green; `/data/roms` absent because no `player` group existed and tmpfiles skipped every `player player` rule (design D2); fixed with `users.groups.player`, the account's primary group, and `admin` in it. Run 3 (2026-08-30, PR #1, run 33297213694): green; all 23 subtests, 106 s of test script under KVM
- [x] 2.5 Only if the machine-id assertion in 2.4 fails: add the initrd oneshot service from design D2 to `modules/persistence` (`DefaultDependencies=no`, after `sysroot.mount`, before `initrd-nixos-activation.service`; `mkdir -p` both parents, touch `/sysroot/persist/etc/machine-id` and `/sysroot/etc/machine-id` if absent, `mount --bind` the former over the latter), rerun 2.4, and update D2 to say the fallback is in force

## 3. Secrets

- [x] 3.1 Generate the test host key `tests/test_host_ed25519_key{,.pub}` (`ssh-keygen -t ed25519 -N ""`, committed, labelled as test-only) and add a `secrets/test\.yaml$` creation rule to `.sops.yaml` whose only recipient is `ssh-to-age < tests/test_host_ed25519_key.pub` (design D7 test secrets contract)
- [x] 3.2 Create `secrets/test.yaml` with `wifi_ssid`, `wifi_psk`, `admin_password_hash` (the values from `tests/values.nix`, design D7; no new hash is generated), editing it with the D7 `SOPS_AGE_KEY=$(ssh-to-age -private-key -i tests/test_host_ed25519_key)` invocation
- [x] 3.3 Admin-side (this Mac), before anything references `secrets/secrets.yaml`: ensure `~/.config/sops/age/keys.txt` exists (`age-keygen` if absent); generate the host key at `~/.config/emubox/ssh_host_ed25519_key`; put both public recipients into a `.sops.yaml` creation rule whose `path_regex` is `secrets/secrets\.yaml$` (not `secrets/.*`: sops takes the first matching rule and a broad one would capture `test.yaml`); create `secrets/secrets.yaml` with the three keys, using placeholder values where the real SSID, PSK or password are not yet known, and note in `secrets/README.md` that placeholders must be replaced before install (the file only has to exist and be valid sops for eval to pass)
- [x] 3.4 In `modules/secrets`: `sops.defaultSopsFile = ../../secrets/secrets.yaml`, declare `sops.secrets.admin_password_hash` with `neededForUsers = true`, declare `sops.secrets.wifi_ssid` and `wifi_psk`, and `sops.templates."wifi.env"` per design D3
- [x] 3.5 In `modules/recovery`: `users.users.admin.hashedPasswordFile = config.sops.secrets.admin_password_hash.path`
- [x] 3.6 Test module per design D7: `sops.defaultSopsFile = lib.mkForce ../secrets/test.yaml`, `sops.age.sshKeyPaths = lib.mkForce [ ./test_host_ed25519_key ]`, `sops.age.keyFile` left null, `services.openssh.enable = lib.mkForce false` (no host key in the VM, so sshd's key generation would fail on the bind); all at the module's top level, not under `disko.tests.extraConfig`, so they reach the `closure-no-secrets` root as well as the installed system
- [x] 3.7 VM assertions: secrets exist on the runtime path (`/run/secrets*`) with the declared mode and with content compared in Python against `tests/values.nix` (`machine.succeed("cat <path>").strip() == value`; never a guest-shell `grep` of the value, per design D7); the `closure-no-secrets` check from 1.1 passes (the store is not grepped from inside the VM); `admin` logs in on tty1 with the `password` from `tests/values.nix` (`wait_until_tty_matches("1", "login: ")`, send the credentials, `wait_until_tty_matches("1", r"admin@.*\$")`, each with an explicit timeout; the display manager is forced off in the test module per design D7, so tty1 carries a getty)

## 4. Networking

- [x] 4.1 In `modules/hardware`: `networking.networkmanager.ensureProfiles.profiles.family-wifi` per design D4 with `environmentFiles = [ config.sops.templates."wifi.env".path ]`
- [x] 4.2 VM assertions: `nmcli connection show family-wifi` succeeds, the generated `.nmconnection` under `/run/NetworkManager` contains the `psk` from `tests/values.nix` (the store is covered by the `closure-no-secrets` check from 1.1, not grepped in the VM), every TCP listener from `ss -ltnH` is on a loopback address, every UDP listener from `ss -lunH` other than the DHCP client ports 68 and 546 is on a loopback address, and `modules/hardware` asserts at eval, next to the firewall so it guards the shipped configuration and not only the test variant, that `networking.firewall.allowedTCPPorts`, `allowedUDPPorts`, `allowedTCPPortRanges` and `allowedUDPPortRanges` are all empty and `networking.firewall.interfaces == {}` (with `services.openssh.openFirewall = false`, since nixpkgs opens port 22 by default)

## 5. Base system leftovers

- [x] 5.1 WirePlumber HDMI-default fragment and `node.restore-default-targets = false` per design D5 in `modules/hardware`
- [x] 5.2 `services.journald.extraConfig` with `SystemMaxUse=256M` and `MaxRetentionSec=1month`
- [x] 5.3 VM assertions: WirePlumber fragment present under the wireplumber config dir, `nix-gc.timer`, `nix-optimise.timer`, `fstrim.timer` active, zram swap present, `RuntimeWatchdogUSec` is 30 s, `systemctl suspend` is refused, `timedatectl` shows America/New_York, and `systemd-analyze cat-config systemd/journald.conf` shows `SystemMaxUse=256M` and `MaxRetentionSec=1month`

## 6. Install runbook and recipes

- [x] 6.1 `justfile` per design D8: `secrets-edit`, `secrets-rekey`, `test-secrets-edit` (`SOPS_AGE_KEY=$(ssh-to-age -private-key -i tests/test_host_ed25519_key)`), `host-key <path>` (generate if absent), `install <target> [nixos-anywhere args]` (the host key path comes from `EMUBOX_HOST_KEY` or the default; refuses placeholder secrets; stages `persist/etc/ssh/ssh_host_ed25519_key{,.pub}` and runs `nixos-anywhere --flake .#emubox --extra-files`); no `deploy` recipe, that is E10's
- [x] 6.2 README install section: prerequisites (age key, host key, installer ISO with root SSH access enabled, Ethernet, Secure Boot off), the command, building from macOS via the Linux builder with `--build-on remote` as fallback, post-install checklist (secrets decrypt, `nmcli` shows the profile, `/root` marker gone after reboot, `admin` login), a note that push deploy arrives with E10 and what to do until then (design D8), reinstall and disk-swap path, and the note that a missing `/persist` or `/data` stops the boot in the initrd's emergency mode by design
- [x] 6.3 `secrets/README.md`: both files, their recipients (`secrets.yaml`: admin age key and host key; `test.yaml`: the committed test host key `tests/test_host_ed25519_key` converted with ssh-to-age, only; values are not secrets and are plaintext in `tests/values.nix`), the key list, and the placeholder note

## 7. Close out

- [x] 7.1 Remove every `TODO(design 4)` and `TODO(secrets)` comment that this change resolved; leave later-epic TODOs in place
- [x] 7.2 `.github/workflows/ci.yml`, landed before the first push: before the nix step, add the udev step that makes `/dev/kvm` openable by the build users (rule `KERNEL=="kvm", GROUP="kvm", MODE="0666"`, then `udevadm control --reload-rules && udevadm trigger --name-match=kvm`); without it the daemon advertises `kvm` so the test is scheduled, but `nixbld` cannot open the 0660 root:kvm device, QEMU's `accel=kvm:tcg` silently falls to TCG and the run crawls into the driver's timeout
- [x] 7.3 Verification actually performed (local pre-flight green before each push; CI green on run 3 of PR #1, 2026-08-30, after two red runs that each found a real defect, see 2.4), in two halves (design Context and Migration Plan). Local pre-flight, on the Mac and its OrbStack builder, before the push: `just check-all` green (its `nix flake check` skips the `x86_64-linux` checks), `nix build .#checks.x86_64-linux.vm.driver` green (the test script renders, type-checks and lints), `just closure-check` green. CI, the VM test's runner: the repository gets a GitHub remote, `main` and the change branch are pushed and a pull request is opened from the branch (the admin's actions; the workflow triggers on `pull_request`), and the job is green with 7.2 in place; CI is the authority from then on. A red first run is read from the job log and fixed on the branch: a driver timeout means `/dev/kvm` was not openable (7.2), an assertion failure is a finding against the module or the test, a runner out of disk space would mean the closure plus the test disks outgrew the `/nix` volume `nothing-but-nix` provides (about 65 GB at `holster`; the first run measured about 85 GB free before any step, so this is not expected) and the `hatchet-protocol` level needs raising
- [x] 7.4 Walk each scenario in the six delta specs and confirm it is either asserted by the VM test or documented as hardware-only or design-guaranteed
- [x] 7.5 Binary cache per design D8 (cache created, token and key in place; run 3 pushed the four cores, about 98 MB, and `emubox.cachix.org` serves them): `cache-roots` flake output, `cachix-action` (pull-only) plus the explicit push step in `ci.yml`, `extra-substituters` gated on `emubox.facts.binaryCachePublicKey` in `modules/hardware`, `just cache-push`, README note (all in the tree). Admin chores, outside the repo: create the public `emubox` cache on app.cachix.org, add its write token as the `CACHIX_AUTH_TOKEN` repository secret, set `binaryCachePublicKey` in `hosts/emubox/facts.nix`; then a CI run shows the push step uploading and the next run substituting the cores instead of compiling them
