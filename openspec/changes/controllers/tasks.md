## 1. Slot identity and the session hint

- [x] 1.1 Add `environment.sessionVariables.SDL_JOYSTICK_DEVICE` to `modules/controllers/default.nix`, the `emubox-pN` paths joined with `:` in `emubox.facts.controllerPorts` order, guarded so nothing is declared when the list is empty; verify by evaluating the host configuration (whose ports are empty) and asserting the variable is absent, and evaluating a fixture with three ports and asserting the joined value and its order
- [x] 1.2 Add `tests/controllers.nix` as a plain node built from the same modules, with `emubox.facts.controllerPorts` overridden to fixture device paths; the node carries the boot adaptations the plain node in `tests/kiosk.nix` already makes, without which a node built from these modules never boots - `rollback-root.service`, `persist-dirs.service` and `persist-machine-id.service` suppressed in the initrd, since otherwise the initrd drops to emergency; tmpfs stand-ins for `/persist` and `/data` with `neededForBoot`; sops pointed at the committed test secrets file and test host key; and sshd turned off - preferably by factoring them out of `tests/kiosk.nix` into one shared test module both nodes import, so the two cannot drift, since a missing adaptation surfaces only when the VM test runs, which for this project is in CI, and so costs a full CI round to discover; the node has no graphical session, because it turns one off: the boot adaptations leave the display manager and its autologin on, and the kiosk session runs `emubox-prepare` on every loop before starting the frontend, which on this node would run the editor alongside, and between, the test's own runs, so the node sets `services.displayManager.sddm.enable = lib.mkForce false`, as the install node in `tests/default.nix` does, on the controllers node itself rather than in the shared boot-adaptation module, since the kiosk node needs its session; with it off, nothing on the node runs the configuration editor, whose only caller on the box is the kiosk session script: the test runs the node's own `emubox-prepare` as `player`, with `ESDE_APPDATA_DIR` set, against the node's rendered owned-values file taken from the node's configuration through `emubox.kiosk.ownedValuesFile` rather than rebuilt by the test, in the manner `tests/kiosk.nix` already does with its `OWNED_VALUES` input, its manual `emubox-prepare` invocation and its `rerun_prepare` helper, for the first run and for every re-run and recreate the later tasks check; the node has no btrfs snapshot layer, so its backups section reports the local snapshot layer as not yet run and warns, and no assertion on this node expects a successful aggregate exit status - aggregate success is asserted on the install node, in 2.5; register it in `flake.nix` under the host-only check set beside `kiosk`, `session` and `retroarch-settings` rather than among the per-system checks, since a NixOS VM test offered per-system would be evaluated for a system it cannot be built for; add the one host-only check this change introduces - this node - to the `nix eval` enumeration the justfile's `eval` recipe drives, since that hand-maintained list of derivation paths is the only thing that evaluates host-only checks on a machine that cannot build them, and a check missing from it lets an evaluation error in the new node pass every local gate and fail in CI; verify the node boots to its assertions, that no `emubox-session` or `es-de` process is running on it before the first owned-key check, that the check is offered for `x86_64-linux` only, that `nix flake check` still evaluates on a non-Linux system, and that the local gate fails on a non-Linux machine when the new node is given a deliberate evaluation error and passes again once it is removed
- [x] 1.3 Add the fixture to `tests/controllers.nix`: a test-only udev rule ordered ahead of the module's own that sets `ID_PATH` to each fixture string and `ID_INPUT_JOYSTICK=1` on the fixture pads created through `uinput` and on no other device, so the non-joystick input device 2.6 creates stays unmarked; verify the node asserts every `emubox-pN` resolves to its fixture device in recorded order
- [x] 1.4 Assert the session hint in `tests/controllers.nix` against `/etc/set-environment`, covering both its presence and its port order; and assert its absence from the live session environment in `tests/kiosk.nix`, whose node imports the real facts and so records no ports, extending the existing `/proc/<pid>/environ` assertion

- [x] 1.5 Add a `controllers-test` recipe to the justfile beside `vm-test` and `kiosk-test`, building the controllers check with the same `--no-link` and carrying the same comment about needing an `x86_64-linux` builder that exposes KVM, so the third VM test has the documented recipe every VM test owes and is runnable on its own rather than only as part of a full flake check; verify `just --list` offers it beside the other two and that it builds the controllers check on such a builder

## 2. Operator status aggregator

- [x] 2.1 Add `pkgs/emubox-status` with its unit tests: it reads the reporter list from a path given on its command line, runs each reporter in declared order, labels each section with its capability, exits with the worst state any reporter returned, treats exit statuses 0, 1 and 2 as ok, warn and fail respectively, treats any other exit status as the reporter having failed to run, and detects a reporter whose command is missing or not executable as having failed to run before invoking it - a reporter that could not run being `fail` for its own section without suppressing the others - and runs each reporter's registered argv exactly as given, adding nothing to and prepending nothing onto the program search path it passes on, so a reporter's own runtime closure is the registering module's responsibility; verify the unit tests cover each of those behaviours, including the missing-command and out-of-range-status cases separately and a reporter whose command exists and is executable but shells out to a program the inherited path does not carry, asserting the aggregator neither supplies that program nor rewrites the path and that the section carries whatever status the reporter itself returned; register the package's tests in `flake.nix`'s per-system check set beside the other helper packages so they run natively in `just check-all`; and add the package to `pkgs/default.nix` beside the other programs this project writes, verifying it is reachable through the overlay on the host's package set, offered as a standalone `x86_64-linux` package output, and present among the store paths the cache roots gather
- [x] 2.2 Add `modules/status/default.nix` as the status capability's own module, one directory per concern as every other capability in `modules/` has: it declares the `emubox.status.reporters` option, renders the registered list to `environment.etc."emubox/status-reporters"`, and puts `emubox-status` on the system path invoking that stable path with no arguments; and add `./status` to the imports list in `modules/default.nix`, since a module directory that list does not name is never evaluated, which would leave the option undeclared and every module registering a reporter against it failing host evaluation on an undefined option; verify by evaluating a configuration with two registered reporters and asserting the rendered file's content and order, that the option is declared by that module rather than by any capability that registers against it, and that evaluating the real host configuration yields both the rendered reporter list at its stable path and the aggregator on the system path, so the import is proved rather than assumed
- [x] 2.3 Give `emubox_restic_backup.py` a way to limit `--status` to the local snapshot layer, leaving the default `--status` report and its exit statuses exactly as they are, and add unit tests for both forms: the default reporting all three layers, the limited form reporting the local layer only and neither off-site layer; and add a unit test proving the limited form healthy: with a current local invocation and a valid local marker, it exits 0, reports the local recovery point, and its output carries neither off-site layer and no off-site warning; verify the helper's existing unit tests still pass unchanged
- [x] 2.4 Register the backups reporter from `modules/backups/default.nix` outside the `lib.mkIf cfg.enable` branch that installs the helper, naming the helper by store path as `modules/saves` already does for the local marker so it does not depend on that branch's `environment.systemPackages`: the local-layer-only form where off-site backup is disabled, the full `emubox-restic-backup --status` where it is enabled, since `btrbk-local` is declared unconditionally in `modules/saves` while the off-site units exist only under that guard; add systemd to the runtime path `pkgs/emubox-restic-backup/package.nix` prefixes onto the wrapped helper, whose current entries are btrfs, restic and util-linux alone, so the helper's `systemctl` and `journalctl` calls resolve when the aggregator runs it as a subprocess rather than from an administrator's shell; and remove the `emubox-status` alias from `pkgs/emubox-restic-backup/package.nix` and the `argv[0]` dispatch from `emubox_restic_backup.py`; verify no derivation still produces a second `emubox-status` binary, that the wrapped helper's `--status` reports its layers rather than raising a file-not-found error when it is run with a program search path that carries neither `systemctl` nor `journalctl`, that evaluating a configuration with off-site backups disabled renders a backups reporter limited to the local layer, and that evaluating one with them enabled renders the three-layer form
- [x] 2.5 Update the three `emubox-status` call sites in `tests/default.nix`, whose node enables off-site backups, so each says which command it means: the `machine.succeed("emubox-status")` site in the maintenance-and-status block stays on the aggregator, now asserting the aggregate is healthy across every reporter that node registers - which is sound only because that node records no controller ports and a box recording none is reported without warning, so the controllers reporter is `ok` there and the aggregate can succeed; state that dependency where the assertion is, so it is a stated precondition rather than an accident of the current reporter's behaviour; the two `machine.fail("emubox-status")` sites that match on `restic-backups-emubox.service: latest invocation failed` become `machine.fail("emubox-restic-backup --status")` with their substring assertions unchanged; and the first of those two additionally runs the aggregator and asserts the same substring appears under the backups section label, so the labelling is proved once without repeating it. Leave `tests/backups.nix` alone: its assertions are evaluation-time and name no command, so nothing in it changes; verify the install test still asserts the same health outcomes it asserted before, and that the added aggregator assertion names the section label
- [x] 2.6 Add the controllers status reporter, listing which recorded ports resolve to a connected pad and which resolve to none, and warning for any controller whose vendor and product identifiers match none of the accepted modes, a controller being an input event device udev marks `ID_INPUT_JOYSTICK=1`, the property the port rule already matches, and its identifiers being the vendor and product the input device itself reports rather than udev's USB-derived `ID_VENDOR_ID` and `ID_MODEL_ID`, which a `uinput` device has no USB parent to supply, so no other input device is classified or named; an empty slot is information rather than a finding, so a recorded port that resolves to no pad and a box that records no ports at all are both reported without warning and leave the section healthy, making an unaccepted mode the only condition under which this reporter is unhealthy; declare the accepted set as a list in the module rather than a single constant, so a further mode can be added later without reworking the reporter, holding the wired pad's identifiers alone - `045e:028e`, the mode it presents under the in-kernel `xpad` driver and the identity the packaged RetroArch autoconfig profile keys on; register the reporter through `emubox.status.reporters`; verify the reporter's own classification with controlled device identities and slot resolution - a connected recorded port and a disconnected one, a pad in an accepted mode producing no warning, a pad in an unaccepted mode named in the warning, an unaccepted-mode joystick outside the recorded slots also named, since the warning covers every controller the system sees, and an input device udev does not mark as a joystick, a keyboard-only device created through `uinput` carrying an unaccepted vendor and product that the fixture rule leaves unmarked, not named, with the controllers section's own status `ok` while no unaccepted-mode joystick is present - asserting each finding through the aggregated `emubox-status` output as well as through the section's presence; assert also the status the reporter returns for an empty slot, since a warning for one would make the status command unsuccessful on a healthy box for the life of that box: on this node, for a recorded port that resolves to no pad, that the controllers section reports the port as unoccupied and that the section's own status is `ok` with no warning - the section's status rather than the aggregate exit status, which is not asserted here because this node has no btrfs snapshot layer, so its backups section reports the local layer as not yet run and warns; and for a node that records no ports at all, that the section says no ports are recorded and raises no warning, the aggregate success on such a node being the install node's assertion in 2.5 rather than repeated here; and that a reporter that fails to run still leaves the other sections produced, with its own section reported as not having run and the aggregate exit status that of `fail` - which is what a reporter that did not run counts as - rather than merely unsuccessful, since the backups section's warning already makes it unsuccessful on this node; that reporter is test-only, registered on this node and switchable at runtime, its registered command being a mutable path outside the read-only store that the test controls - while the test has made that path non-executable, or removed it, the reporter cannot be executed at all, rather than being a script that runs and exits with a status outside 0, 1 and 2, so the aggregator reports it as not having run rather than as having returned `fail`, the other sections are still produced, and the aggregate exit status is that of `fail`; and while that path holds an executable, the reporter runs and reports `ok` - so it fails to run only for the assertion that needs it and cannot make the node's other assertions unsuccessful
- [x] 2.7 Give `tests/controllers.nix` a test-only second reporter registered through `emubox.status.reporters` - the switchable reporter 2.6 registers, with its command in place and executable so it reports `ok`, rather than a separate one - and assert one `emubox-status` run carries it, the controllers section and the backups section together, without asserting that run's exit status successful, since this node has no btrfs snapshot layer and its backups section warns that the local layer has not yet run; verify that node, which leaves off-site backups off, has its backups section reporting the local snapshot layer and neither off-site layer, and that the run carries no section for a capability that registered no reporter at all, which is where the "a capability contributes no report" case actually occurs now that the backups reporter is registered on every box

## 3. Standalone emulator input ownership

- [ ] 3.1 Determine, for each of Dolphin, Azahar, PCSX2, DuckStation, PPSSPP and ScummVM at the version this flake pins, taking the unit of determination to be the emulator together with every system the frontend assigns it rather than the emulator alone: which configuration file holds the bindings for each of those systems, its file name read from the emulator's own source or from a file its writer produced, since an emulator serving more than one system may keep a separate controller profile per system and a system whose profile is left unowned launches with nothing bound - Dolphin is the known case, serving both the GameCube and the Wii systems, and its Wii profile is owned on the same rule that governs its GameCube one; which input interface that emulator enumerates controllers through at the pinned version, recorded with the source location it was read from beside the emulator's other findings, since the enumeration order the session declares reaches only an emulator that reads controllers through the system's game controller library and an emulator with an input backend of its own is outside that guarantee; the same input-interface question for RetroArch, the frontend's core-based emulator, at the version this flake pins, although this change gives it no other controller configuration: its effective joypad driver, recorded with where that was read, since the tree sets none and upstream RetroArch's `configuration.c` defaults to the `udev` joypad driver whenever the build has udev support, that driver being left as it is; where that interface is an input backend of the emulator's own rather than the system's game controller library, RetroArch's included, whether that backend can list both a recorded port's name link and the event node the link resolves to, and, if it can, how the owned binding keeps that pad from being presented twice - the system's game controller library needing no such answer, since SDL3 drops a device node it has already listed by comparing device numbers, so a port name and its event node are one entry there; for RetroArch, whose bindings this change does not own, a backend of its own means the recorded port order is not promised for it, and that limit is recorded; whether its pad defaults are compiled into the emulator as fallbacks for bindings its configuration file does not carry or are written into that file when it first runs; whether the flake owns the file they land in; whether its own defaults bind an Xbox-360-class pad for gameplay at all; whether its configuration can express a simultaneous two-button route back to the frontend or only an alternative of single inputs; and how many players each of its systems supports locally, counted by that system's native controller ports without a multiplayer accessory such as a multitap, which gives PlayStation and PlayStation 2 two players, so that each further player that system supports gets bindings and a player it does not support gets none; for each system and each player, whether that player's controller slot is connected by default and, where it is not, which file and which setting connect it - Dolphin's further GameCube ports and Wii Remotes, PCSX2's second pad and DuckStation's second port being the cases already read as disconnected by default; for the route back, which bindable action ends the emulator's process under the command the frontend launches it with, whether that action raises a confirmation by default and which setting suppresses it, and whether reaching the frontend needs a launch option - PPSSPP's pause menu Exit reachable only under `--pause-menu-exit` being the case already read, and Dolphin, PCSX2, DuckStation and ScummVM each confirmed to exit to the frontend when emulation stops, with whether their stop raises a confirmation of its own; Azahar's route-back question is already answered and is recorded as such: at the pinned version it has no pad-bindable exit, since it persists a hotkey as a key sequence and a context and nothing else (`src/citra_qt/uisettings.h`, `ContextualShortcut`; `src/citra_qt/configuration/config.cpp`, `ReadShortcutValues` and `SaveShortcutValues`, which read and write only the key sequence) and the `controller_keyseq` member of its in-memory hotkey structure (`src/citra_qt/hotkeys.h`) is never set by `hotkeys.cpp` and never saved, so its route back is deferred to a later change and recorded beside its key table with those source locations rather than bound; for each emulator whose configuration file the configuration editor creates before that emulator first runs, whether its first start treats that file as foreign - resetting it, prompting about it or rewriting it - and on which key's absence, which is then the acceptance key 3.2 enforces, PCSX2's `UI.SettingsVersion` being the case already read; for each binding the flake will own, route back and gameplay alike, any setting outside the binding value that the binding depends on to reach a pad, and so the tier it is owned in - Dolphin's per-section `Device` line, which that section's bindings resolve against, being the case already read, and Azahar's QSettings `profiles` array another: the array's count key, its active-profile index key and the per-profile key prefix its bindings sit under, each in on-disk spelling, `profiles\size`, `profile` and `profiles\1\` being the expected answer, since Azahar 2125.1.2's `QtConfig::ReadControlValues` reads the array whose length the count key gives and reads the flat legacy keys only when that count is 0, together with which of those keys its writer gives a `\default` companion; for every key and value the flake will own, its on-disk spelling, read from a file the emulator itself wrote or from a round-trip through that emulator's own writer - Azahar's QSettings writer, with its backslashed `\default` companions, its percent-encoded key names and its double-quoted comma-bearing values, being the case already read; for every value the flake will own, whether it names the pad itself rather than an input on it - a device selector naming the backend's own name for the device, or a binding value carrying an SDL joystick GUID - and whether the emulator offers a form that names no particular device, such as a device index, which is then the form used; and, from those, for each emulator whether its gameplay bindings depend on a pad-identity fact, which puts them and every setting they depend on to reach a pad in the enforced tier, Azahar's, whose values carry the pad's SDL joystick GUID, being the case already read; an identity-bearing value's spelling may come from a round-trip, but its content is taken only from the real pad under Linux at bring-up, since a value produced on the development machine names the wrong device; the tool that reads it there, recorded by its nixpkgs package name: `sdl-jstest` or an equivalent nixpkgs tool that prints SDL's device names and joystick GUIDs, the SDL joystick GUID being the form Azahar's bindings carry and the SDL device name, which Dolphin's SDL backend uses, the form wherever a Dolphin `Device` line must name the pad; and, for each identity-bearing key, confirmation that the tool's output matches the form the emulator stores, the spelling rule applied to the tool's output, with the evidence it was confirmed from; produce the full emulator-by-system-by-player matrix rather than assuming which emulators need extra players, stopping the matrix at player four whatever a system supports beyond that, since the box records four controller ports and a fifth player's binding names a pad that cannot reach the box, and record the finding and its source location per emulator - naming which of the two default shapes it found, the file name of each owned file new to the editor with the source or writer-produced file it was read from, which input interface it enumerates through and, for a backend of its own, whether it can list a pad twice and how the owned binding avoids it, which setting connects each further player's slot, which action its route back is bound to, which setting suppresses that action's confirmation and which launch option it needs, what its first start requires of a prepared file, which settings each binding depends on to reach a pad and so the tier each is owned in, the on-disk spelling of each owned key and value with the file or round-trip it was read from, which owned values carry the pad's identity and whether an identity-free form exists for each, which tool captures each identity value and the evidence that its output matches the form stored, whether its gameplay bindings therefore depend on a pad identity, and where each of those was read - as comments beside the key tables written in 3.2 and 3.3, and, for RetroArch, which has no key table of its own, its effective joypad driver, where that was read and any port-order limit, beside the controllers module's key tables, so the evidence lands in the tree rather than in this list. This determination is the gate for every per-emulator fact the key tables need - the names of the files new to the editor, slot switches, exit actions and their confirmations, launch options, acceptance keys, the settings a binding depends on such as device selectors, on-disk spellings, which values carry the pad's identity, and the tool that captures them: 3.2 to 3.4 consume only what it records, each fact with its source location or round-trip evidence, and the implementation's per-group review checks its matrix before those tasks begin; the design's emulator table is its expected output, to be confirmed here, not a closed list to stop at
- [x] 3.2 First add `emubox.emulators.configDirs` to `modules/emulators/default.nix`: an internal, read-only option exposing each standalone emulator's configuration directory, derived from the same bindings that module already uses for its own `ownedFiles` entries so the two modules' paths cannot drift, and changing no emulator behaviour; the key tables in this task and in 3.3 build every file path they contribute from it, so a misspelled path cannot give a file that carries every owned key, passes every check and is read by nothing. If 3.1 finds any owned value that must carry the pad's identity, also declare `emubox.facts.controllerIdentities` in `modules/facts.nix` beside `controllerPorts`, an attribute set holding each identity value 3.1 names, and declare it empty in `hosts/emubox/facts.nix` with a `TODO(bring-up)` comment, as its port paths are, the comment naming the capture tool 3.1 records as the port paths' comment names `udevadm info`; and add that tool's nixpkgs package to `environment.systemPackages` from `modules/controllers/default.nix`, so it is on the box's system path where the administrator works at bring-up, verifying that the built host system carries it by evaluating the host configuration and asserting that package is among its system packages. In the same module as `configDirs`, add `--pause-menu-exit` to the frontend's PSP launch command (`pspOverride`), so PPSSPP's pause menu offers an Exit that ends the program in place of its exit to PPSSPP's own menu; and own, in the **enforced** tier of that emulator's owned-file table, the acceptance key 3.1 names for every emulator whose first start rejects a configuration file this system prepared, each beside that emulator's other enforced keys with a source comment in the style the neighbouring keys use - PCSX2's `UI.SettingsVersion = "1"` among them, its comment naming `CheckSettingsVersion` in `pcsx2/VMManager.cpp` and the reset question in `pcsx2-qt/QtHost.cpp`, since the configuration editor creates that file before PCSX2 first runs; 3.4 pins them. Then contribute enforced route-back bindings for the five standalone emulators other than Azahar as `emubox.kiosk.ownedFiles` entries from `modules/controllers/default.nix`, each bound to the action 3.1 names as ending that emulator's process under the frontend's launch command, using per-emulator bindings where a uniform combination is not expressible, each with the reason from 3.1; Azahar gets no route-back binding, since 3.1 records it as having no pad-bindable exit at the pinned version; own each exit-confirmation suppression setting 3.1 names in the **enforced** tier, at a path built from `emubox.emulators.configDirs`, and none for Azahar; own, in the same **enforced** tier, every setting 3.1 names that a route-back binding depends on to reach a pad, Dolphin's `Hotkeys.ini` `[Hotkeys]` `Device` line among them; add PPSSPP's `controls.ini` as its own owned-file entry with its own `format`, distinct from the `ppsspp.ini` the emulators capability already owns, alongside Dolphin's `GCPadNew.ini`, its Wii profile `WiimoteNew.ini` - owned on the same rule as the GameCube profile, since the frontend launches the Wii system through Dolphin too and half the systems Dolphin serves would otherwise have no owned route back - and `Hotkeys.ini`, each with its own `format`, together with any further per-system profile 3.1 names; declare every key and value in the spelling 3.1 records from that emulator's own writer; declare each value in the identity-free form 3.1 finds wherever one exists, and otherwise take the pad's identity from the `emubox.facts.controllerIdentities` entry 3.1 names, declaring neither a key whose entry is still empty nor any route-back binding that depends on it, and nothing in its place, so neither a placeholder identity nor another machine's value is ever written; verify `tests/controllers.nix` asserts the configuration of every standalone but Azahar carries its enforced binding, with each setting 3.1 names that binding depends on to reach a pad, and that a configuration altered to unbind the route back, or to change a setting the route back depends on to reach a pad, has the binding and that setting restored the next time the configuration editor runs - on this node the test's own `emubox-prepare` run that 1.2 names, standing in for the run before the frontend starts; verify also that `tests/controllers.nix` asserts each exit-confirmation suppression setting, and that the frontend's PSP launch command carries `--pause-menu-exit`, and that a configuration altered to turn the confirmation back on is restored by the next editor run, made the way 1.2 names; and verify that evaluating the host configuration shows every `ownedFiles` path `modules/controllers` contributes lies under one of the directories `emubox.emulators.configDirs` exposes, and that `modules/controllers/default.nix` contains no literal emulator configuration path; and verify that `tests/controllers.nix` hand-types the full expected path of every owned file new to the editor that this task and 3.3 contribute - Dolphin's `GCPadNew.ini`, `WiimoteNew.ini` and `Hotkeys.ini`, PPSSPP's `controls.ini` and any further file 3.1 names - taking each file name from the evidence 3.1 records of the emulator's own file names rather than from the flake's declaration, and asserts that this set equals the set of owned-file paths new to the editor in the node's rendered owned-values document, the directory check above kept beside it, since the node's other checks read their paths from that same document and so agree with a misspelled file name, and wherever a file's keys wait on a pad-identity fact the kiosk node's hand-typed pins leave them out, so no other check types that file's name by hand; and, for each key whose value carries a pad-identity fact, verify that `tests/controllers.nix`, whose node records a fixture value for each such fact beside its fixture ports, asserts the key carries that value, and that evaluating a configuration whose identity facts are empty declares none of those keys and none of the route-back bindings depending on them
- [x] 3.3 Contribute gameplay bindings as `emubox.kiosk.ownedFiles` entries, at file paths built from `emubox.emulators.configDirs` as 3.2's are, in the **seeded** tier except for a binding 3.1 finds depending on a pad-identity fact, which is declared in the **enforced** tier together with every setting it depends on to reach a pad - Azahar's bindings among them, and Dolphin's wherever 3.1 finds a gameplay `Device` line must name the pad itself - since an emulator that writes its own values into those keys before bring-up records the fact, as Azahar does each time it saves its configuration, would keep a seeded binding out for good; under the rule 3.1 establishes, applied to each configuration file that determination names rather than once per emulator, so an emulator serving more than one system is covered for each of them - Dolphin's Wii profile `WiimoteNew.ini` alongside its `GCPadNew.ini`: for an emulator whose pad defaults are compiled-in fallbacks, only the bindings its defaults leave unbound, and those for each further player that file's system supports up to player four and none for a player it does not support; for an emulator whose pad defaults are written into a configuration file the flake owns, the complete gameplay binding set for every player that file's system supports up to player four, whatever a pristine install would have produced for those players and nothing for a player beyond the fourth, since the editor recreates a missing or unreadable owned file carrying the owned keys and nothing else and an unowned default in that file is therefore erasable - PPSSPP's `controls.ini` being the confirmed case, where the loader drops every mapping the file omits; declare the additional players' bindings unconditionally, not gated on `emubox.facts.controllerPorts`, since the binding names a controller index the emulator assigns rather than a port the configuration records; for Azahar, declare its bindings under the `profiles` array's per-profile prefix 3.1 records, with the array's count key and active-profile index key beside them as settings the bindings depend on to reach a pad, in the bindings' enforced tier and not as read-gating flags, since Azahar reads the array whose length the count key gives, falls back to its flat legacy keys only when that count is 0, and writes the array back on every save; declare an owned `<key>\default = false` in the same section beside every controller key owned there that 3.1 finds Azahar's writer flagging so, spelled with the backslash QSettings' INI writer emits and placed in the **enforced** tier whatever tier its binding sits in, since that flag decides whether the value is read at all rather than expressing a preference and a stale `true` discards a player's own binding as readily as the flake's; declare every key and value this task owns in the writer's spelling 3.1 records, as 3.2 does, Azahar's double-quoted comma-bearing binding values and any percent-encoded key name among them; declare each value in the identity-free form 3.1 finds wherever one exists, and otherwise from the `emubox.facts.controllerIdentities` entry 3.1 names, declaring neither a key whose entry is still empty nor any gameplay binding that depends on it, nor any setting such a binding depends on, and nothing in its place, since a placeholder would be written as if it were right; own, beside the bindings they serve and in those bindings' tier, every setting 3.1 names that a gameplay binding depends on to reach a pad, the `Device` lines of Dolphin's gameplay profiles and Azahar's array keys among them; contribute, for every further player the flake binds whose controller slot the emulator leaves disconnected by default, the setting 3.1 names that connects that slot, in the tier of the bindings it makes usable, in whichever file holds it - including `Dolphin.ini`, `PCSX2.ini` and DuckStation's `settings.ini`, which the emulators capability owns - at paths built from `emubox.emulators.configDirs`, declared unconditionally like those players' bindings, and enabling no multitap or other multiplayer accessory; verify, with every editor run made the way 1.2 names, that `tests/controllers.nix` asserts every required binding appears when absent, player one's and each additional player's alike, Azahar's enforced `\default` companion beside each of its owned keys that 3.1 finds flagged so a binding written without one fails the check, the `profiles\size` and `profile` keys with the flake's values beside Azahar's bindings in the file the editor creates, and each such slot setting, and each setting a binding depends on to reach a pad, beside the bindings it serves, on a node whose recorded ports are empty as well as on the fixture node, each key whose value carries a pad-identity fact, and each binding depending on such a key, being expected only where that fact holds a value and checked as 3.2 checks its own, that a file-generated emulator's owned file carries the complete set for every player its system supports up to the fourth, and no binding for a fifth, after the editor recreates it from nothing, and that an altered seeded binding of each emulator that has one, one depending on no pad-identity fact, and an altered seeded slot setting, survive a further editor run; and that a readable owned file holding a binding for a player beyond the declared set keeps that binding after an editor run while a missing owned binding in the same file is written, since the player limit bounds what the flake declares rather than what an existing file may hold; verify also, for Azahar, starting from a binding altered away from the flake's value whose `\default` companion has been set to `true`, that an editor run restores the flake's binding and sets the companion to `false`, and that a second editor run then leaves the file unchanged; and verify the transition an identity fact makes when bring-up records it: run the editor first against the owned-values file rendered from a variant of the node's configuration whose identity facts are empty, taken from that configuration's `emubox.kiosk.ownedValuesFile` as 1.2 takes the node's own; then write the emulator's own values, any values other than the flake's, into every identity-bearing key, every binding depending on one and every setting such a binding depends on to reach a pad, as an emulator that runs and saves its configuration before bring-up does, and alter one seeded binding that depends on no pad identity; then run the editor again against the node's own owned-values file, whose identity facts hold their fixture values, and assert that after that run every one of those identity-dependent keys holds the flake's value while the altered seeded binding keeps its altered value, and that a further editor run against the same file changes nothing
- [ ] 3.4 Add the new enforced and seeded controller keys, with hand-typed expected values, to `tests/kiosk.nix`'s `PINNED_OWNED_KEYS_ENFORCE` and `PINNED_OWNED_KEYS_SEED` tables, Dolphin's Wii profile keys alongside its GameCube ones, the enforced exit-confirmation suppression settings, every setting a binding depends on to reach a pad in its binding's tier, Dolphin's `Device` lines among them, and the slot settings in the tier of the bindings they serve, those contributed into files the emulators capability owns among them, included, together with every acceptance key the emulators module gains in 3.2, PCSX2's enforced `UI.SettingsVersion` among them; a gameplay binding that depends on a pad-identity fact, every setting it depends on to reach a pad, Azahar's `profiles` array keys, and Azahar's `\default` companions whatever tier their bindings sit in, are enforced keys, pinned in `PINNED_OWNED_KEYS_ENFORCE` and never in the seed table wherever they are pinned; hand-type each key and value in the writer's spelling 3.1 records rather than copying the flake's declaration, Azahar's double-quoted comma-bearing values included, so a declaration in any other spelling fails the pin; the tables carry no Azahar route-back key and no `confirmClose`, since Azahar has no owned route back at the pinned version; and a key whose value carries a pad-identity fact is left out of them, with every binding that depends on it and every setting such a binding depends on to reach a pad, since the kiosk node imports the real facts, which leave that fact empty until bring-up, so none of them is declared there - Azahar's bindings, their companions and its array keys among them, and Dolphin's gameplay bindings with their `Device` lines and slot settings wherever 3.1 finds those lines must name the pad itself - and they are asserted in the enforced tier on the controllers node by 3.2 and 3.3 instead; and extend that file's on-disk walk - the loop over every file in the rendered owned-values document that follows the `check_pins` calls, whose enforced branch compares each key's on-disk value against the flake's declared value while its seeded branch asserts presence alone - so that its seeded branch compares a seeded key's on-disk value against the flake's declared default as its enforced branch already does; `check_pins` itself needs no change, since it reads the rendered owned-values contract rather than the disk and already compares both tiers by value - on this node nothing has changed a seeded value, so the flake's default is what the file must hold, and presence alone would pass a seeded binding written with the wrong value exactly as it would the right one; verify the extended walk fails when a seeded key's on-disk value is altered away from the flake's default before it runs, and that the kiosk test's own "a seeded frontend setting changed while the frontend is stopped survives a reboot" assertion still passes, since it exercises a different file and a deliberate change. `tests/owned-key-tiers.nix` needs no change and none should be made: its candidates are derived from the rendered configuration rather than enumerated, so it already covers the new keys' enforce/seed exclusivity unchanged - state that in the change rather than editing the file

## 4. Documentation and hardware checklist

- [x] 4.1 Document the broadened scope of `emubox-status` in `README.md`: correct the line that describes `emubox-status` as the backup status command and say what its backups section holds on a box with off-site backup off, namely the local snapshot layer alone and neither off-site layer; describe `emubox-status` as aggregating the reports registered with it - on this box the backups and controllers reports - rather than presenting it as the whole box's health, and say the BIOS check remains its own command, `emubox-check-bios`; and correct the line calling the two RetroArch combos "the only controller-only routes out of a running game", which now holds for the core-based frontend alone, since every standalone emulator but Azahar gains its own owned route back, and the corrected line says Azahar has none at the pinned version rather than implying every emulator can be left with a pad, and, for any emulator whose route back 3.1 finds waiting on a pad-identity fact, that its route back arrives once bring-up records that fact; state, for each emulator 3.1 records the port-order limit for, RetroArch included, that the recorded player order does not reach it; and correct every place the README states the box has two VM tests - the development section's sentence naming `just vm-test` and `just kiosk-test` as the two, the repository-layout line that lists the tests by name, and the backups section's line naming those same two as the recipes needing a KVM builder - so each names the third VM test and its recipe alongside them; verify the README no longer describes `emubox-status` as backup-only or as reporting the whole box's health, describes it as aggregating the registered reports and names `emubox-check-bios` as a separate command, states three VM tests wherever it enumerates them and names the new recipe, and no longer claims the RetroArch combos are the box's only controller-only routes out of a game, and that it states the port-order limit for every emulator 3.1 records one for
- [x] 4.2 Add the controller items to the hardware bring-up checklist in `README.md` as bullets in the plain list that section already is - it has no table and no per-item marker, and the only table in the file is the separate off-site backup acceptance record, which this task does not touch - each written in the shape the existing items use: a pad in every port; for each input backend 3.1 finds an emulator enumerating controllers through, RetroArch's and the system's game controller library among them, with the recorded ports active, one connected pad enumerating as exactly one controller and two connected pads as exactly one controller each, in recorded order wherever 3.1 does not record the port-order limit for that backend, since a pad reached through both its port name and its underlying device node is to be presented once; the three player-order items that follow, each checked in an emulator the recorded order reaches, one whose port-order limit 3.1 does not record, while for each emulator whose port-order limit 3.1 records, RetroArch included, each item records the order observed there instead of expecting the order it states: a pad connected mid-game, which takes the lowest free player index whatever its port; a pad disconnected and reconnected mid-game, which likewise takes the lowest free player index and so regains its former index only when no lower one is free - with players one and two both disconnected and player two's pad reconnected first, that pad becomes player one; and the documented "second port first, then the first" sequence, in which a pad in the second port alone is player one and a pad then connected to the first port mid-game takes player two, the lowest free index, until the next game launch applies the recorded order; per standalone emulator other than Azahar, the pad plays and the route back ends the emulator and returns to the frontend with no prompt in between, while for Azahar the pad plays and the item records that leaving it needs the route back deferred to a later change, and, for each emulator whose first start 3.1 finds rejecting a prepared file, PCSX2 among them, its first start on a freshly prepared box raises no settings prompt, and, with two pads, player two's pad plays in a two-player game on each emulator whose systems support two or more players; with a pad in a recorded port, `emubox-status` reports that slot resolved and raises no unaccepted-mode warning, and if it does warn, the vendor and product pair the pad reports is recorded and added to the module's accepted set, since that set holds the one mode this change has evidence for and a pad presenting another is a bring-up finding rather than a fault; and whether the wired pad persists its mode across disconnects. Do not add an item for recording the four port paths into `hosts/emubox/facts.nix`: the checklist's first item already carries those paths alongside the HDMI connector, and that section states there is no second list, so a second item would be exactly the duplication that statement rules out; where 3.1 finds owned values that must carry the pad's identity, extend that first item instead, so that beside the port paths it records each such value, read from the real pad under Linux with the capture tool 3.1 records, which the item names, and copied in the form that tool prints, which 3.1 confirmed is the form the emulator stores, into `emubox.facts.controllerIdentities`, since a value taken on the development machine names the wrong device and one derived by hand passes every check downstream, and so that it names itself the gate for pad play and the route back on each emulator whose owned values need such a fact, since until it is recorded those values and every binding depending on them are undeclared, and that emulator's per-emulator item is checked only after it, staying the end-to-end check that the recorded value is right; verify each added item reads as a bullet in that list in the same shape as the existing ones, that the single-presentation item covers every input backend 3.1 finds, RetroArch's included, that the three player-order items expect the order they state only in an emulator the recorded order reaches and record the order observed in each emulator whose port-order limit 3.1 records, that the checklist still carries exactly one item for the recorded port paths, with any identity values beside them, the capture tool named, and named as the gate for those emulators' pad play and route back, and that no table is added to the section

## 5. Gate

- [ ] 5.1 Run `just check-all` and `just session-check` clean, and confirm CI runs the new `controllers` check, whose documented local recipe on a KVM builder is the new `just controllers-test`; verify by a green CI run on the change branch whose log shows the controllers VM test executing rather than being skipped
