## 1. VM test harness on the real layout

- [x] 1.1 Create `tests/values.nix` per design D7 (plaintext `ssid`, `psk`, `password` and `hash`, the hash generated once with `nix shell nixpkgs#mkpasswd -c mkpasswd -m yescrypt`, since `mkpasswd` is not in the devShell; test-only, not secrets); rewrite `tests/default.nix` as a NixOS module that imports it and sets `disko.tests.extraChecks` and leaves `disko.tests.extraConfig` empty (the harness fixes the booted VM at 1 GB RAM and a 600 s timeout and exposes no memory option, so none is set); point `checks.x86_64-linux.vm` at `(nixosConfigurations.emubox.extendModules { modules = [ ./tests ]; }).config.system.build.installTest` in `flake.nix`, and add `checks.x86_64-linux.closure-no-secrets` per design D7 (grep over `closureInfo` rooted at that same `extendModules` expression's `config.system.build.toplevel` for the `psk` and `hash` imported from `tests/values.nix`); every test override lives at the test module's top level and `disko.tests.extraConfig` stays empty
- [ ] 1.2 Write the first assertion group in `extraChecks`, with every boot phase (initial, after `start()`, after `crash()`/`start()`) beginning with `machine.wait_for_unit("multi-user.target")` because the harness itself only waits for `local-fs.target`: `/`, `/nix`, `/persist`, `/data`, `/data/cache` are btrfs subvolume mounts, `bootctl status` reports systemd-boot, `id player` succeeds, `/data/home/player` exists, and each of `/data/{home,roms,bios,saves,es-de,media,cache}` exists with the owner and mode the tmpfiles layout declares; confirm `just vm-test` runs the install test on the KVM-capable builder (prerequisite from design Context; the derivation requires the `kvm` feature) within the harness's 1 GB / 600 s budget (a Plasma-sized overrun on KVM triggers the design's Risks fallback, forcing the display manager off at the test module's top level; a timeout under TCG means the builder is not KVM-capable and is not that trigger)
- [x] 1.3 Add the persistence assertions (red): record `/etc/machine-id`, write `/root/marker`, `/etc/marker`, `/data/marker`, `/tmp/marker`, `shutdown()`/`start()`, assert `/root`, `/etc` and `/tmp` markers gone, `/data` marker present, machine-id unchanged, `journalctl --list-boots` shows two boots, and `findmnt --target /var/log` reports a SOURCE ending in `[/@persist/var/log]` (on btrfs findmnt shows the subvolume path in brackets, never a `/persist/...` path)
- [x] 1.4 Add the power-cut assertion: write `/root/marker` again, `crash()` then start, node reaches `multi-user.target` with no repair prompt and the marker is gone

## 2. Ephemeral root

- [ ] 2.1 Verify in the VM that the root partition carries disko's `disk-main-root` label and that `btrfs` is on the initrd PATH; if not, add `boot.initrd.systemd.extraBin.btrfs`
- [x] 2.2 Add `boot.initrd.systemd.services.rollback-root` to `modules/persistence` per design D1 (mount top level by partlabel, delete nested subvolumes, delete and recreate `@root`)
- [x] 2.3 Drop the RSA host key entries from the persisted list and set `services.openssh.hostKeys` to ed25519 only; keep the directory list as scaffolded
- [ ] 2.4 Run the VM test on the KVM builder: 1.3 and 1.4 go green
- [x] 2.5 Only if the machine-id assertion in 2.4 fails: add the initrd oneshot service from design D2 to `modules/persistence` (`DefaultDependencies=no`, after `sysroot.mount`, before `initrd-nixos-activation.service`; `mkdir -p` both parents, touch `/sysroot/persist/etc/machine-id` and `/sysroot/etc/machine-id` if absent, `mount --bind` the former over the latter), rerun 2.4, and update D2 to say the fallback is in force

## 3. Secrets

- [x] 3.1 Generate the test host key `tests/test_host_ed25519_key{,.pub}` (`ssh-keygen -t ed25519 -N ""`, committed, labelled as test-only) and add a `secrets/test\.yaml$` creation rule to `.sops.yaml` whose only recipient is `ssh-to-age < tests/test_host_ed25519_key.pub` (design D7 test secrets contract)
- [x] 3.2 Create `secrets/test.yaml` with `wifi_ssid`, `wifi_psk`, `admin_password_hash` (the values from `tests/values.nix`, design D7; no new hash is generated), editing it with the D7 `SOPS_AGE_KEY=$(ssh-to-age -private-key -i tests/test_host_ed25519_key)` invocation
- [x] 3.3 Admin-side (this Mac), before anything references `secrets/secrets.yaml`: ensure `~/.config/sops/age/keys.txt` exists (`age-keygen` if absent); generate the host key at `~/.config/emubox/ssh_host_ed25519_key`; put both public recipients into a `.sops.yaml` creation rule whose `path_regex` is `secrets/secrets\.yaml$` (not `secrets/.*`: sops takes the first matching rule and a broad one would capture `test.yaml`); create `secrets/secrets.yaml` with the three keys, using placeholder values where the real SSID, PSK or password are not yet known, and note in `secrets/README.md` that placeholders must be replaced before install (the file only has to exist and be valid sops for eval to pass)
- [x] 3.4 In `modules/secrets`: `sops.defaultSopsFile = ../../secrets/secrets.yaml`, declare `sops.secrets.admin_password_hash` with `neededForUsers = true`, declare `sops.secrets.wifi_ssid` and `wifi_psk`, and `sops.templates."wifi.env"` per design D3
- [x] 3.5 In `modules/recovery`: `users.users.admin.hashedPasswordFile = config.sops.secrets.admin_password_hash.path`
- [x] 3.6 Test module per design D7: `sops.defaultSopsFile = lib.mkForce ../secrets/test.yaml`, `sops.age.sshKeyPaths = lib.mkForce [ ./test_host_ed25519_key ]`, `sops.age.keyFile` left null, `services.openssh.enable = lib.mkForce false` (no host key in the VM, so sshd's key generation would fail on the bind); all at the module's top level, not under `disko.tests.extraConfig`, so they reach the `closure-no-secrets` root as well as the installed system
- [x] 3.7 VM assertions: secrets exist on the runtime path (`/run/secrets*`) with the declared mode and with content compared in Python against `tests/values.nix` (`machine.succeed("cat <path>").strip() == value`; never a guest-shell `grep` of the value, per design D7); the `closure-no-secrets` check from 1.1 passes (the store is not grepped from inside the VM); `admin` logs in on tty2 with the `password` from `tests/values.nix` (`send_key("ctrl-alt-f2")`, tty1 being a Wayland compositor, then `wait_until_tty_matches("2", "login: ")`, send the credentials, `wait_until_tty_matches("2", r"admin@.*\$")`; tty1 is held by the SDDM autologin)

## 4. Networking

- [x] 4.1 In `modules/hardware`: `networking.networkmanager.ensureProfiles.profiles.family-wifi` per design D4 with `environmentFiles = [ config.sops.templates."wifi.env".path ]`
- [x] 4.2 VM assertions: `nmcli connection show family-wifi` succeeds, the generated `.nmconnection` under `/run/NetworkManager` contains the `psk` from `tests/values.nix` (the store is covered by the `closure-no-secrets` check from 1.1, not grepped in the VM), every TCP listener from `ss -ltnH` is on a loopback address, every UDP listener from `ss -lunH` other than the DHCP client ports 68 and 546 is on a loopback address, and the test module asserts at eval that `networking.firewall.allowedTCPPorts`, `allowedUDPPorts`, `allowedTCPPortRanges` and `allowedUDPPortRanges` are all empty and `networking.firewall.interfaces == {}`

## 5. Base system leftovers

- [x] 5.1 WirePlumber HDMI-default fragment and `node.restore-default-targets = false` per design D5 in `modules/hardware`
- [x] 5.2 `services.journald.extraConfig` with `SystemMaxUse=256M` and `MaxRetentionSec=1month`
- [x] 5.3 VM assertions: WirePlumber fragment present under the wireplumber config dir, `nix-gc.timer`, `nix-optimise.timer`, `fstrim.timer` active, zram swap present, `RuntimeWatchdogUSec` is 30 s, `systemctl suspend` is refused, `timedatectl` shows America/New_York, and `systemd-analyze cat-config systemd/journald.conf` shows `SystemMaxUse=256M` and `MaxRetentionSec=1month`

## 6. Install runbook and recipes

- [ ] 6.1 `justfile` per design D8: `secrets-edit`, `secrets-rekey`, `test-secrets-edit` (`SOPS_AGE_KEY=$(ssh-to-age -private-key -i tests/test_host_ed25519_key)`), `host-key <path>` (generate if absent), `install <target> [host-key]` (stages `persist/etc/ssh/ssh_host_ed25519_key{,.pub}` and runs `nixos-anywhere --flake .#emubox --extra-files`); no `deploy` recipe, that is E10's
- [ ] 6.2 README install section: prerequisites (age key, host key, installer ISO with root SSH access enabled, Ethernet, Secure Boot off), the command, building from macOS via the Linux builder with `--build-on remote` as fallback, post-install checklist (secrets decrypt, `nmcli` shows the profile, `/root` marker gone after reboot, `admin` login), a note that push deploy arrives with E10 and what to do until then (design D8), reinstall and disk-swap path, and the note that a missing `/persist` or `/data` stops the boot in the initrd's emergency mode by design
- [ ] 6.3 `secrets/README.md`: both files, their recipients (`secrets.yaml`: admin age key and host key; `test.yaml`: the committed test host key `tests/test_host_ed25519_key` converted with ssh-to-age, only; values are not secrets and are plaintext in `tests/values.nix`), the key list, and the placeholder note

## 7. Close out

- [ ] 7.1 Remove every `TODO(design 4)` and `TODO(secrets)` comment that this change resolved; leave later-epic TODOs in place
- [ ] 7.2 `.github/workflows/ci.yml`, landed before the first push: before the nix step, add the udev step that makes `/dev/kvm` openable by the build users (rule `KERNEL=="kvm", GROUP="kvm", MODE="0666"`, then `udevadm control --reload-rules && udevadm trigger --name-match=kvm`); without it the daemon advertises `kvm` so the test is scheduled, but `nixbld` cannot open the 0660 root:kvm device, QEMU's `accel=kvm:tcg` silently falls to TCG and the run hits the 600 s timeout
- [ ] 7.3 Verification actually performed: `just check-all` green on the Mac (its `nix flake check` skips the `x86_64-linux` checks); `just vm-test` and `nix build .#checks.x86_64-linux.closure-no-secrets` green on the KVM-capable builder; CI green once the repository has a remote, with 7.2 in place (CI is the authority from then on, its first run is not gated here, and it is red between 1.3 and 2.4 by design)
- [ ] 7.4 Walk each scenario in the six delta specs and confirm it is either asserted by the VM test or documented as hardware-only or design-guaranteed
