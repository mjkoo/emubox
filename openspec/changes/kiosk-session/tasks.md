## 1. emubox-prepare (red first: the editors' tests exist before the editors)

- [x] 1.0 Add a `LICENSE` file at the repo root carrying the MIT licence in the author's name (design D7: the repo has carried none, which leaves every consumer of the public cache without a grant for the project's own programs). `git grep -n -i "license\|licence" -- README.md` returns one line, DuckStation's CC-BY-NC-ND sentence, which is about a vendored package and does not contradict the repo's own licence
- [x] 1.1 Create `pkgs/emubox-prepare/` with `ruff.toml` (`extend-select = ["ARG"]`, nothing that replaces the defaults), `package.nix` (design D3: `stdenvNoCC.mkDerivation` with `python3` as a build input, stdlib-only Python 3.13, installs `bin/emubox-prepare`, `nativeCheckInputs` of `ruff`, `ty`, `python3.pkgs.pytest`, `doCheck = true` so the `checkPhase` actually runs, that phase running `ruff check .`, `ruff format --check .`, `ty check`, `pytest`), `meta` with `license = lib.licenses.mit` and a `description`, as both vendored siblings carry (design D7), and an empty `emubox_prepare.py`; add `emubox-prepare = pkgs.callPackage ./emubox-prepare/package.nix { }` to `pkgs/default.nix`, the file form its siblings use, since `callPackage` on a bare directory would import a `default.nix` this change does not create. Widen that file's header too: it says today that the directory holds what the project builds because the pinned nixpkgs no longer carries it, and `emubox-prepare` is a program of this project's own that nixpkgs never carried. The packages delta's requirement "The flake's own programs are package outputs too" states everything the flake owes this program; this entry is part of satisfying it, and task 3.3 verifies it
- [x] 1.2 Write `test_emubox_prepare.py` for the three editors (design D3) against temporary files: ES-DE XML (create with owned keys; set a drifted key; append a missing key; leave an unowned element and its position untouched; typed `<bool>`/`<int>` elements), INI with sections (comments, order and unknown keys preserved; key set in the right section; section created if absent), RetroArch flat file (same properties), and "no write when nothing changed" for each; the recreate policy (design D3): a malformed ES-DE XML document truncated mid-element is replaced by one carrying every owned key, an unreadable INI or RetroArch file likewise, and a settings path whose parent directory does not exist is written after the editor creates the parents; plus the contract's failure mode from design D3, the one whose consequence is the greeter: with `ESDE_APPDATA_DIR` unset or empty the program writes nothing and exits non-zero with a message on stderr, and the recreate policy does not cover it; plus the custom-systems step (copy on difference, no-op when equal, copy into a `custom_systems/` directory that does not yet exist, remove when the path argument is the empty string, and a no-op rather than an error when the path argument is empty and no file is there). Confirm the tests fail against the empty module
- [x] 1.3 Implement the editors and the `main()` that reads the owned-values JSON and the custom-systems path from the command line and applies them under `ESDE_APPDATA_DIR`; `nix build .#checks.aarch64-darwin.emubox-prepare` (after 3.2 wires the check) or a direct `python3 -m pytest` in the package directory passes, and `ruff check`, `ruff format --check` and `ty check` pass. `ty check` passing is required, not optional: per-rule downgrades in a `ty.toml` are the allowed lever (design Risks), skipping the check is not, so this task is not done until ty is green

## 2. ES-DE kiosk quit menu patch

- [x] 2.1 Write `pkgs/es-de/kiosk-quit-menu.patch` against v3.4.1 `es-app/src/guis/GuiMenu.cpp` (design D2): the QUIT entry added regardless of UI mode; `openQuitMenu()` skips the "Quit ES-DE" and "Suspend" rows when `ForceKiosk` or `UIMode == "kiosk"`; add `patches = [ ./kiosk-quit-menu.patch ]` and the header sentence naming upstream commit `defb16b6` and what the patch changes
- [x] 2.2 `nix build .#packages.x86_64-linux.es-de` succeeds on the Linux builder (recompiles ES-DE once; the version hook and find-rules guard still pass); prove the guard by building with the patch's context line altered (an `overrideAttrs` that substitutes one hunk, or a temporary edit restored from the index) and watching the patch phase fail; record the command here
  - Built clean on the OrbStack builder: `nix build .#packages.x86_64-linux.es-de --no-link --print-out-paths` -> `/nix/store/cazh36v32n7yzvr1mi3j36zfc2wlf0qg-es-de-3.4.1`, the version hook and the find-rules grep both passing inside it.
  - The guard is proved by altering a line the patch **removes**, not a context line. A context line altered to `addEntry(_("UTILITIES-THAT-DO-NOT-EXIST"), ...)` still built: GNU patch's default fuzz factor is 2, and tolerating cosmetic drift *around* a hunk is the behaviour we want, since the hunk still applies where it means to. Changing the removed line to `getBool("ForceKioskRenamedUpstream")` - which is what a bump that moved the code the patch targets looks like - fails as required: `applying patch /nix/store/...-kiosk-quit-menu.patch` / `Hunk #1 FAILED at 82.` / `1 out of 4 hunks FAILED -- saving rejects` / `builder failed with exit code 1`. The patch was restored from the pristine a/b copies afterwards and rebuilt to the same store path as above.

## 3. Module, flake, formatter

- [x] 3.1 `modules/kiosk`: add `options.emubox.kiosk.passkey` (default `"uuddlrlrba"`) and `options.emubox.kiosk.customSystems` (default `""`) per design D6; render the owned-values JSON (`UIMode=kiosk`, `UIMode_passkey`, `ROMDirectory=/data/roms`, `MediaDirectory=/data/media`, `Theme=linear-es-de`, `ApplicationLanguage=en_US`, `ShowQuitMenu=true`, with ES-DE's element types) and, when `customSystems` is non-empty, the custom-systems file with `writeText` (design D6); set `services.displayManager.sddm.autoLogin.relogin = false` explicitly with the design D1 comment; add `options.emubox.kiosk.appdataDir` (path, `readOnly`, `internal`, `/data/es-de`) and `options.emubox.kiosk.ownedValuesFile` (path, `readOnly`, `internal`, defaulted to the rendered JSON) and put `emubox-prepare` in `environment.systemPackages` beside `cage` and `es-de`, which is both how the session script's own call resolves rather than through `runtimeInputs` and what satisfies the packages requirement's program-path scenario (design D3's invocation contract, D6); rewrite `emubox-session` per D1 and that contract: `export ESDE_APPDATA_DIR=${cfg.appdataDir}` once at the top before the loop rather than as a prefix on any single command, mode check, `emubox-prepare <owned-values-json> <custom-systems-path>` with exactly two positional arguments and the second the empty string when the option is empty, `cage -- es-de` (no `--force-kiosk`) with its exit status captured rather than discarded with `|| true` so `set -e` does not end the session (design D1), run-length measurement, the counter with `EMUBOX_CRASH_WINDOW` (default 60) and three strikes, `exit 0` at the threshold (this task was written as `exit 1`; CI proved that wrong - SDDM reads a session exiting 1 as `HELPER_AUTH_ERROR` and then never starts a greeter at all, so the box would sit on a black screen. Design D1 carries the mechanism), 2 s pause otherwise; `TODO(design 4)` removed, `TODO(design 9)` and `TODO(design 11)` kept
- [x] 3.2 `flake.nix`: add `python3`, `ruff`, `ty` and `python3.pkgs.pytest` to the devShell beside the existing `sops`, `actionlint` and `cachix` tooling, so tasks 1.2 and 1.3 have the commands they call (`nix fmt` already reaches ruff through the formatter override, but the shell reaches none of the four); narrow the unfree-core selection feeding `cache-roots` so it admits only cores every one of whose licences is redistributable - today's `isUnfree` reads `meta.license.free`, a different field from `redistributable`, and admits a core the cache must not carry (design D7). The predicate runs over `lib.toList (p.meta.license or [ ])`, a list, so the new one is `lib.all (l: l.redistributable or l.free or false)` and **not** `lib.any` as the neighbouring `isUnfree` uses: `any` would admit a core one of whose licences forbids redistribution, which is the hole the guard exists to close. `nix eval` of `cache-roots` before and after records which cores, if any, the narrowing drops, and that record goes here - a drop is a finding about that core's licence, not a failure of this task; `checks.${system}.emubox-prepare` for every system via `pkgsFor`, reached on darwin with `(pkgsFor system).callPackage ./pkgs/emubox-prepare/package.nix { }` rather than by importing `./pkgs`, and merged with the existing host-only `checks.${hostSystem}` attrset rather than assigned beside it, so `x86_64-linux` carries both (design D4); the prose describing `pkgs/` and the cache is swept by task 5.4, not here; `formatter` becomes `nixfmt-tree.override { runtimeInputs = [ ruff ]; settings.formatter.ruff-format = ...; }` per design D4; `nix fmt -- --ci` passes on the tree (run `nix fmt` once for the Python files); `just check-all` passes on the Mac, which now runs the Python checks
  - Before/after record for the narrowing: the unfree cores the selection picks are `libretro-snes9x`, `libretro-genesis-plus-gx`, `libretro-picodrive` and `libretro-fbneo`. Every one of them carries `meta.license.redistributable = true` (and `free = false`), so **the narrowing drops nothing today** and `cache-roots` has the same eight members before and after. The guard is there for the core that arrives later without anyone looking at its licence.
  - `nix flake check` on the Mac was proved to actually run the Python checks rather than only evaluate them: a deliberately failing assertion in `test_emubox_prepare.py` made it fail with `1 failed, 32 passed`, and the test was restored afterwards. That is the vm-test delta's "Editor checks on macOS" scenario.
  - `nix fmt` reformatted the two Python files on first run; `nix fmt -- --ci` is clean over 28 files. `just check-all` passes on the Mac.
- [x] 3.3 Every scenario of the packages delta's "The flake's own programs are package outputs too" is exercised on the Linux builder, that requirement being where the list lives: `nix build .#packages.x86_64-linux.emubox-prepare` succeeds, `nix eval .#packages.x86_64-linux.cache-roots` (or the built linkFarm's contents) shows its store path among the roots; the program-path scenario is proved in task 4.2, on the only node this change boots; `just build` succeeds on the Linux builder; `nix eval` of the host's `emubox-session` script text shows the prepare call, the `ESDE_APPDATA_DIR` export above the loop, no `--force-kiosk`, and an empty second argument to `emubox-prepare` for the host's empty `customSystems`, which is the out-of-VM half of design D5's empty-case proof

  - `nix build .#packages.x86_64-linux.emubox-prepare` -> `/nix/store/4hzls39cwp4qfa95i5c3fs8j4cmfjhjq-emubox-prepare-0.1.0`, and the built `cache-roots` linkFarm holds `emubox-prepare-0.1.0 -> 4hzls39cwp4qfa95i5c3fs8j4cmfjhjq-...`, the same store path, beside the three vendored packages and the four unfree cores.
  - `just build` -> `/nix/store/wi83pj0nd0742x4brx8ly5z9qdpybl92-nixos-system-emubox-26.05.20260827.d57af92`.
  - The host's `emubox-session` text, read from the store path the session desktop entry's `Exec` names, shows `export ESDE_APPDATA_DIR=/data/es-de` once above the loop, `emubox-prepare /nix/store/...-emubox-owned-values.json ""` with the empty second argument the host's empty `customSystems` renders, `cage -- es-de` with no `--force-kiosk`, and `|| rc=$?` rather than `|| true`.
## 4. Kiosk VM test

- [x] 4.1 Create `tests/kiosk.nix` per design D5 and Context: node imports `nixosModules.emubox` and `hosts/emubox/facts.nix`, the initrd persistence units disabled, `/persist` and `/data` tmpfs `neededForBoot`, test sops file and key forced, sshd forced off, 2 GB, `-vga none -device virtio-gpu-pci`, `EMUBOX_CRASH_WINDOW` set to 10 via `environment.sessionVariables`, which SDDM's PAM stack exports into the `player` session (design D5), a non-default `emubox.kiosk.passkey` so the option's wiring is provable, an `emubox.kiosk.customSystems` stub that is a complete `es_systems.xml` document, `<systemList>` wrapper included, holding one system (design D6: the module writes the option verbatim and adds no wrapper); wire the kiosk check into `flake.nix` as `kiosk = hostPkgs.testers.runNixOSTest (import ./tests/kiosk.nix ...)` among the host-only checks that task 3.2 merges into the per-system `checks` attrset - not as a `checks.${hostSystem}` assignment of its own, which no longer exists after that restructure; `just eval` gains its `drvPath`; `just kiosk-test` added
- [x] 4.2 Write assertions covering every "node driver" row of design D5's coverage table and no scenario outside them - one subtest may cover several rows (the settings-file check answers three) and the boot check takes two, so it is coverage that must hold, not a one-to-one count. That table is the change's single enumeration of what proves each kiosk scenario, so an assertion added or dropped here starts as an edit there: display manager active with autologin proved by an active `player` session on `seat0`, not by the greeter's absence; `es-de` under `cage` within the test's startup budget (120 s of `wait_until_succeeds`, not the spec's 60 s, which is the box's figure); owned keys present with the declared values in `${nodes.machine.emubox.kiosk.appdataDir}/settings/es_settings.xml`, `UIMode_passkey` among them carrying the module's non-default passkey; custom systems file equals the stub, then the removal branch proved on the same node, before any `pkill` (design D5: a later loop iteration would recreate the file), by running `ESDE_APPDATA_DIR=${nodes.machine.emubox.kiosk.appdataDir} emubox-prepare ${nodes.machine.emubox.kiosk.ownedValuesFile} ""` as `player` - as a bare command name, which is also what proves the packages requirement's program-path scenario - and asserting the file is gone; relaunch after `pkill -x es-de` once the run exceeded the lowered window, proved by PID rather than by presence - record the PID, kill, `wait_until_fails` on it, then a *different* `es-de` PID within 15 s (design D5; 15 s is the kiosk spec's own constant, asserted as written, unlike the 60 s startup figure the test replaces with its 120 s budget, because a relaunch on this node is fast and the spec's number needs no headroom); three fast kills, each inside the lowered window, then after a grace period longer than the loop's 2 s pause, no `es-de` across repeated checks with `display-manager.service` still active as the primary assertion (design D5: without the grace period the assertion is momentarily true after any kill), the greeter proved by an active `sddm` session on `seat0` (this task was written as "the greeter's process name only as a secondary check"; that observable was dropped, as design D5 allowed, after nixpkgs' wrappers made the names `.cage-wrapped` and `.kwin_wayland-w` and `pgrep -f` proved vacuous - D5 carries the replacement); `machine.reboot()` last, after the three-kill subtest, so the reboot is genuinely from the greeter, then the session comes up again; `nix build .#checks.x86_64-linux.kiosk.driver` renders the script on the Linux builder
  - `nix build .#checks.x86_64-linux.kiosk.driver` -> `/nix/store/sxx2nzdlrq2gh73k0pwm1ql0qd2977iq-nixos-test-driver-emubox-kiosk`. The driver type-checks the test script, which caught the real signature of the driver's `retry`: its keyword is `timeout_seconds`, not `timeout`, at all three call sites. The test itself is not run here - it needs KVM, which this project has recorded it does not have locally, so a green run is task 4.3's CI record.
  - Four defects found by the review wave before the test ever ran, three of which would have failed CI. Design D5 is updated for each, since the test derives from it. (1) The node disabled the three initrd persistence units with `enable = false`, which *masks* a unit rather than removing it while still emitting its `.requires` links - and `modules/persistence` declares `requiredBy = [ "sysroot.mount" ]`. systemd refuses to enqueue a job requiring a masked unit, so the node would have dropped to initrd emergency and panicked before any assertion ran. Now `boot.initrd.systemd.suppressedUnits`; verified by dumping the generated initrd units, where references to the three units go from three `ln -sfn ... .requires/` lines to zero. (2) `machine.reboot()` needs `allow_reboot`, which the driver does not set by default, so the reset would have ended QEMU; now `shutdown()`/`start()`, the shape the install test already uses. (3) The three-kill loop issued `pkill -x es-de` in a tight loop, which re-signals the process still unwinding from the previous kill - three passes, one crash recorded, session never gives up; now each kill is by pid and waited out. (4) The greeter check used `pgrep -f`, which matches the driver's own `bash -c` command line and so succeeded on any booted machine; now `pgrep -x kwin_wayland`, this pin's greeter compositor, plus an assertion that the seat is no longer `player`'s.
  - `EMUBOX_CRASH_WINDOW` is 30, not the 10 first written: `started=$SECONDS` is set before `cage -- es-de`, so a run's measured length includes cage and ES-DE starting under llvmpipe. The test now also asserts the variable reached the session, by reading `/proc/<cage>/environ`, before either timing subtest relies on it.
  - The node seeds one dummy ROM file, so no assertion depends on how ES-DE behaves with no game files at all.
- [x] 4.3 With the author's go-ahead, open the pull request so CI runs both VM tests (KVM is CI-only); iterate on the kiosk test until green (design Risks list the expected adjustments: wait time, resolution, memory, greeter process name); record the run here
  - PR #3 opened 2026-08-30 (`https://github.com/mjkoo/emubox/pull/3`). Run 1 (`33337692210`) got further than expected: the node **booted**, `display-manager.service` came up, `player` held an active session on `seat0`, and `es-de` was running under `cage` **5.5 s** after the wait began - so the initrd `suppressedUnits` fix, the session loop, autologin and the compositor all work on the first real execution, and the 120 s budget has enormous headroom against the box's 60 s figure.
  - Run 1 failed on the crash-window probe, which was the assertion's own bug rather than the thing it asserts: `pgrep -x cage` matches nothing because nixpkgs ships cage through a wrapper, so its `comm` is `.cage-wrapped`, and the command degenerated to `/proc//environ`. (The ancestry walk passed at the same moment because it substring-matches.) Now read off the frontend's own pid, which inherits the same environment and whose `comm` `pgrep -x es-de` already matches.
  - Run 2 (`33338122274`): seven of the eight subtests pass, including the custom-systems removal branch run as `player` by bare command name, which is also the packages capability's program-path scenario, and the relaunch proved by pid. It failed in the three-kill subtest: a `kill` on a frontend four seconds old was still alive 60 s later. A starting ES-DE has SDL's SIGTERM handler installed but is not yet polling the event queue, so the `SDL_QUIT` it posts sits unread. Those three kills now use SIGKILL, which is in any case the better model of the crash this subtest is about; the relaunch subtest keeps SIGTERM against a frontend that has been up 35 s, which is the graceful-exit path the spec's "menu quit" means.
  - Run 3 (`33338744249`) proved the crash counter end to end - three SIGKILLs, three distinct pids each relaunching in 2-3 s, the session exiting, SDDM logging it - and both primary assertions passed. The secondary greeter check then timed out, which turned out to be a **product bug this test existed to find**, not a brittle observable.
  - Run 4 (`33339332564`) added diagnostics and settled it: after the session ends there are no logind sessions, `seat0` has none, the only process left is the `sddm` daemon itself, and the SDDM journal stops dead at `Auth: sddm-helper exited with 1` with no second "Adding new display..." ever - for over two minutes. The box would sit on a black screen, which is the exact failure state design D1 chose SDDM to avoid.
  - Cause, read from the SDDM 0.21.0 source: `Auth::Private::childExited` casts the helper's exit code *straight to* `Auth::HelperExitStatus` (`src/auth/Auth.cpp:233`), whose value 1 is `HELPER_AUTH_ERROR`, and `Display::slotHelperFinished` is `if (status != Auth::HELPER_AUTH_ERROR) stop()` (`src/daemon/Display.cpp:524`). SDDM refuses to restart a display after an authentication failure so a wrong password cannot loop the greeter - so a session exiting 1 is indistinguishable from a failed login, `stopped` is never emitted, `Seat::displayStopped` never runs, and no greeter is ever created. The session script now exits **0**, which is `HELPER_SUCCESS`; design D1's "the script exits non-zero" was wrong and is corrected there.
  - Run 5 (`33340163002`) **confirmed the fix**: the journal now reads `Auth: sddm-helper exited successfully` / `Removing display ...` / `Adding new display...` / `Greeter starting...` / `Greeter session started successfully`, with `Sessions=c1` on seat0 and the greeter's processes running as user `sddm`. The greeter appears. Only the test's own matcher still failed, for the same reason cage's did: nixpkgs wraps these binaries, so the compositor's `comm` is `.kwin_wayland-w` after the kernel's 15-character truncation, not `kwin_wayland`.
  - The greeter is therefore asserted as an active `sddm` session on `seat0`, which is what "the greeter is shown" means and depends on no binary's name. Design D5 allowed the process-name observable to be dropped if it proved flaky; it proved so twice, and D5 now records the better assertion.
  - **Run 6 (`33340860454`) is green**, the whole job: both VM tests, the closure check, the per-system `emubox-prepare` checks, the format check and the cache push. All nine kiosk subtests pass - display manager and `player` on seat0 (2.0 s), es-de under cage (7.5 s), the crash window reaching the session, the owned keys, custom systems present and removed, relaunch after a long run (39.7 s), three short runs ending at the greeter (34.9 s), and a reboot from the greeter restoring the kiosk (38.9 s). Test script total 158.7 s.
  - Of the design's four anticipated adjustments, only the greeter observable was needed. Wait time, resolution and memory were never touched: ES-DE starts in 5-9 s under llvmpipe against a 120 s budget, and 2 GB was enough. The two real findings were the session's exit code (a product bug, above) and the wrapped-binary `comm` names.

## 5. Specs and docs

- [x] 5.0 Update `tests/default.nix`'s header comment, which ends "The graphical session is proven on hardware" and goes stale the moment `tests/kiosk.nix` exists; `git grep -n "proven on hardware" -- ':!openspec/changes/archive/'` returns `tests/default.nix:24` and nothing else, so no other live prose claims the graphical session is unproven in CI - archived change artifacts are records and are not rewritten
- [x] 5.0b Reword the Purpose of `openspec/specs/packages/spec.md` on this branch so it covers what the flake builds and caches, its own programs included, rather than only what the pinned nixpkgs stopped carrying; the requirements themselves reach the main spec through the delta at archive
- [x] 5.1 Reword the Purpose of `openspec/specs/vm-test/spec.md` on this branch so it covers automated proof both in a VM and outside one: the install test's boot path, the kiosk node's session, and the config editor's checks that run on every system including the Mac, where no VM is involved. Only the Purpose is edited in place - a Purpose has no delta form, while "The test runs in CI and locally" is a requirement and reaches the main spec through the delta's `## MODIFIED Requirements` block at archive (design Open Questions). `openspec validate --strict` still passes
- [x] 5.2 README: a "Kiosk session" section (how the box gets from power-on to ES-DE, the crash counter and the greeter as the failure state, the unlock sequence and `emubox.kiosk.passkey`, which settings the flake owns, `emubox.kiosk.customSystems`, power off from the menu) and the Development section updated for the second test, `just kiosk-test` and the Python checks in `nix flake check` and `nix fmt`; every command the new prose names is run once as written and its output matches what the section claims, except the two that need a KVM builder this project has recorded it does not have locally (`just vm-test` and `just kiosk-test`), whose prose is checked against the CI run task 4.3 records instead
- [x] 5.3 Add a "Bring-up checklist" section to the README - it does not exist yet; E1 left one sentence about the `by-id` disk path and two `TODO(bring-up)` comments in `hosts/emubox/facts.nix` - and fold E1's existing item into it alongside the E12 lines this change defers: boot under 30 s measured power-on to the frontend being usable (distinct from the kiosk spec's 60 s graphical-target figure and from the test's 120 s budget, so the README does not read as three competing deadlines), the TV's native mode, and power off from the patched menu on the real TV. `git grep -n "TODO(bring-up)"` and the new section together account for every deferred hardware item, with none left in a second list
- [x] 5.4 The vendored-packages sweep, which owns every live sentence this change makes false - enumerated here in full rather than found one per review round. `pkgs/` no longer holds only vendored programs, `cache-roots` no longer pushes only vendored store paths and no longer pushes every unfree core (the packages delta's requirements, design D7), and `tests/` no longer holds one test. **Every site below is located by a quoted phrase, never by a line number**: this task runs last, earlier tasks edit these same files above these lines, and a line number written now is stale by the time it is read. Each phrase was checked to occur exactly once in its file.
  - `flake.nix`: the `vendored` binding and its two uses (the name itself, wherever it appears); the overlay comment beginning "Vendored packages (ES-DE, freeimage, DuckStation) live in pkgs/"; the comment beginning "Only for the host's system: the vendored packages are Linux-only", no longer the reason once `emubox-prepare` is deliberately checked on the Mac (D4); and the whole comment block beginning "What the binary cache holds", whose claims that Hydra's gap "is every RetroArch core the emulators module selects that carries a non-free license" and that the cache "is kept licence-clean by what is vendored, not by a filter here" are both falsified by task 3.2's narrowing.
  - `justfile`: "Push the cache roots (unfree cores, vendored packages)".
  - `.github/workflows/ci.yml`: "(the unfree emulator cores, the vendored packages)".
  - `modules/facts.nix`: "emulator cores, the vendored packages)".
  - `modules/hardware/default.nix`: "the unfree cores and the vendored packages" - which also needs the cost sentence, since a host without the cache key builds those paths itself.
  - `overlays/default.nix`: "Vendored packages built against the flake's own nixpkgs".
  - `README.md`: the tree line reading "vendored packages (ES-DE, freeimage, DuckStation)", the tree line reading "VM test module (disko install test)", the cache sentence naming "the unfree emulator cores and the vendored" packages, and the `### Vendored packages` section heading and body. The README also gains a line naming the repo's own MIT licence, which task 1.0 declares in the `LICENSE` file but does not mention in the README.
  - Where these sentences describe what the cache holds, they are re-derived from design D7's three categories rather than reworded in place.

  - Post-condition 1 (singular VM test / two checks), `git grep -n -iE "the VM test|both .*(check|test)" -- ':!openspec/' ':!.scratch/'`: every line it returns read on its own merits (no count is written down here - the task's own text warns a tally goes stale, and one did: a later fix commit added another accurate line). Six were false and were fixed - `.github/workflows/ci.yml` (KVM is needed by both tests now), `tests/default.nix`'s opening line, `justfile`'s test-secrets recipe and `secrets/README.md`'s test.yaml heading and body (the kiosk node consumes the same fixture, so a sentence naming one test for it was as false as one naming one test for the flake), `tests/values.nix`'s header, and one line of the new README bring-up section. Two were loose rather than false and were tightened (`modules/hardware`'s "the VM tests' variants", `modules/persistence`'s markers, which are the install test's specifically). Four were already true in context and were left: `modules/hardware:18` names the shared test instrumentation rather than a test, and `tests/kiosk.nix:9` is this change's own accurate sentence.
  - Post-condition 2 (vendoring), `git grep -n -i vendor -- ':!openspec/' ':!.scratch/'`: seventeen lines, every one accurate about what the flake builds and pushes. The four the task names as sentences about vendoring itself are untouched (`.gitattributes`, CI's disk-space comment, `pkgs/freeimage`'s header, `tests/default.nix`'s subtest banner covering es-de and duckstation), as is `pkgs/default.nix`'s `callPackage` note.
  - `flake.nix`'s `vendored` binding is renamed `ownPackages` at all three of its uses, since the set it names is no longer only vendored.
  - Commands named in the new README prose, each run once as written: `just check-all`, `nix flake check`, `nix fmt`, `nix fmt -- --ci`, `just build`, `just closure-check`. `just vm-test` and `just kiosk-test` need a KVM builder this project has recorded it does not have locally, so their prose is checked against task 4.3's CI run; `just cache-push` needs `CACHIX_AUTH_TOKEN`, which the prose says.

  Both post-conditions below carry the same reach - the whole tree except `openspec/` (the change's own artifacts and the specs, which are edited by their own tasks) and `.scratch/` - so neither can miss a file the other would have caught. A second class of falsified prose shares no word with the first and needs its own post-condition: sentences that call the flake's VM test singular, or its checks two. `git grep -n -iE "the VM test|both .*(check|test)" -- ':!openspec/' ':!.scratch/'` finds them, tree-wide. Some sit in files that describe a fixture rather than a test, and task 4.1 makes the kiosk node consume the same `secrets/test.yaml` and committed test key the install node uses, so a sentence naming one test for that fixture is as false as one naming one test for the flake. Read each line on its own merits; this post-condition names a grep, a pathspec and that instruction, and deliberately no site, count or line - three earlier attempts to enumerate went stale within a round. After this task every line it returns must be true of a flake that defines more than one VM test and more than the checks it defined before - `flake.nix` is where those sets live, and no number is written down here. Read each line rather than rewriting all: some are already fine in context, since `just vm-test` does build one specific test. The README tree line belongs to the first list above, not to this one.

  Verification of the first class is a post-condition too, and deliberately not a count: after the sweep, every line `git grep -n -i vendor -- ':!openspec/' ':!.scratch/'` returns must be accurate about what the flake builds and pushes. Read each and fix or leave it on that test alone. A tally of hits was tried across three review rounds and went stale each time, because earlier tasks add and move prose in these same files before this one runs; the post-condition is what survives that. Sentences about vendoring itself stay as they are - `.gitattributes`, the freeimage and es-de package headers, `pkgs/default.nix`'s `callPackage` note, `.github/workflows/ci.yml`'s disk-space comment, and `tests/default.nix`'s "the vendored programs are installed" banner, which describes a subtest covering exactly `es-de` and `duckstation` and is untouched by this change.
